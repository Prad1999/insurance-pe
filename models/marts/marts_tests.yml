version: 2

models:
  - name: mart_funnel
    columns:
      - name: enquiry_version_key
        tests:
          - not_null
          - unique

  - name: mart_revenue
    columns:
      - name: policy_id
        tests:
          - not_null
          - unique

  - name: mart_retention
    columns:
      - name: policy_id
        tests:
          - not_null
          - unique

  - name: mart_product
    columns:
      - name: policy_id
        tests:
          - not_null
          - unique

  - name: agg_bolt_on_popularity
    columns:
      - name: product_code
        tests:
          - not_null
          - unique